添加新功能只需要在Main.java类中的表里添加映射（记得引入新建的功能类，同时还要新建实体类）
并将新功能仿照login_page写成一个新类即可（注意按页面写类，同一页面不同操作分不同函数写在同一类里）
数据库表中的列可以修改（可能role也是一直要传的）
DataAccessObject中也可能需要更新（如加入数据库更新操作）
User类中可能需要添加一些信息以支持新的操作
可用User来验证身份，其他实体类（如Student）来进行操作
可能有遗漏，可以看代码中的注释